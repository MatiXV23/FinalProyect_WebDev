import { DocumentoValidatorDirective } from './documento-validator.directive';

describe('DocumentoValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DocumentoValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
